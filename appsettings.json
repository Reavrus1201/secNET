{
    "Logging": {
        "LogLevel": {
            "Default": "Debug",
            "System": "Information",
            "Microsoft": "Information"
        }
    },
    "ConnectionStrings": {
        "DefaultConnection": // add own connection string.
    },
    "JwtSettings": {
        "SecretKey": // add own .json secret key.
    }
}